# Gridpoint (Minis System) — السياق

> **اقرأ الملف ده الأول قبل أي شغل.** ده خريطة سريعة — التفاصيل في الملفات اللي بعده.

---

## إيه المشروع

نظام تشغيل للتجارة الإلكترونية **بيتباع كمنتج لأكتر من بيزنس** — كان اسمه
MINIS، وبعدين MINO، ودلوقتي **Gridpoint** (الاسم القديم لسه في اسم الريبو
والدومين، ومالوش تأثير على المستخدم).

أوردرات، عملاء، منتجات ومخزون، مصاريف، خزنة، موردين، مهام، مستخدمين
وصلاحيات — مربوط بشوبيفاي في الاتجاهين (تطبيق عام OAuth + استيراد دوري
+ webhooks) وببوسطة للشحن (مزامنة كل ١٥ دقيقة من كرون الداتابيز).

## الستاك

- **Next.js 16** (App Router) + TypeScript + Tailwind v4 — على Vercel
  ⚠️ دي نسخة Next فيها تغييرات كاسرة — اقرا التنبيه في `AGENTS.md`
- **Supabase** — داتابيز + auth + cron (مشروع واحد، بيزنسات كتير جواه)
- **Vitest** — ١٢٥٠+ اختبار، والفحص الكامل `npm run check`

## الملفات اللي بتشرح — بالترتيب

| الملف | فيه إيه |
|---|---|
| **docs/HANDOVER.md** | المرجع الكامل: إزاي بيشتغل، القواعد، والأخطاء اللي اتعلمناها بالصعب |
| **docs/NEXT.md** | آخر اللي حصل: اللي على الدور، القرارات، **والمواضيع المقفولة** |
| **docs/PLAN.md** | خطة تحويله لمنتج يتباع — المراحل واللي خلص |
| **docs/BACKLOG.md** | القرارات المسجلة وقايمة الميزات الجاية |

## قواعد مش قابلة للنقاش

1. **كل جدول بيزنس فيه `tenant_id`** — ومفتاح الأدمن **بيتعدّى الـRLS**،
   فأي استعلام بيه من غير `.eq("tenant_id", …)` بيشوف كل البيزنسات.
   **الحارس** (`lib/tenant-isolation.test.ts`) بيقرا الكود نفسه وبيوقع
   لو استعلام مكشوف.
2. **حالات الأوردر بتتخزّن بمفاتيح إنجليزي** (`delivered` · `cancelled` …)
   — النص العربي للعرض بس في `ORDER_STATUS_LABELS`.
3. **الإيميلات مبوّبة داخلياً** (`omar+minis@…`) عشان سوبابيز بيتطلب إيميل
   فريد لكل حساب — والعرض بيشيل اللوحة (`displayEmail`). أي كود بيعمل
   حساب لازم يبوّب، وأي عرض لازم يشيل.
4. **ملفات SQL في `sql/`** — عمر بيشغّلها بإيده في سوبابيز (المساعد
   بيكتبها بس)، وكل ملف لازم يخلص بأوامر رجوع.
5. **قبل أي دفع: `npm run check`** — ولو التعديل بيلمس الصلاحيات أو
   العزل: كمان `node scripts/test-isolation.mjs` و
   `node scripts/test-new-tenant.mjs`.
6. **البي آر شغل المساعد** — يعمله، ويستنى الفحص يخضر، ويدمجه بنفسه.
   عمر مش بيدوس ميرج.
7. **فيه مواضيع مقفولة بقرار عمر** — القايمة في آخر `docs/NEXT.md` —
   **متتفتحش ومتتذكرش** لا كطلب ولا كتذكير.

## أسلوب الشغل

- **عربي مصري** — خطوة صغيرة في كل مرة، تجرّبها وتتأكد إنها شغالة.
- **اعرض قبل ما تنفّذ** في أي حاجة بتلمس داتا (`?dry=1` الأول).
- **كل خطوة تتسجل في `docs/NEXT.md`** — عشان الشات الجديد يلاقي كل حاجة
  في مكانها، ده كان طلب عمر الصريح.

---

⚠️ النسخة القديمة من الملف ده كانت بتوصف النظام الأول (بيزنس واحد، ١١
جدول، من غير بوسطة) — اتعادت كتابتها ٢٤ أغسطس ٢٠٢٦ عشان كانت بتضلّل أي
حد بيقراها بعد ما النظام كبر وبقى multi-tenant.
