-- ==========================================================================
-- سجل الأوردر يتربط بالأوردر نفسه مش بتشابه النص
-- ==========================================================================
-- المشكلة: سجل الأوردر كان بيتجمّع بـ
--     summary like '%1359%'
-- ودي بتلقّط حاجات مالهاش علاقة: أوردر ١١٣٥٩، أو مبلغ ١٣٥٩ جنيه في مصروف،
-- أو أي رقم فيه نفس الأرقام. يعني السجل ممكن يوريك حركة على أوردر تاني —
-- وده أوحش من إنه ناقص.
--
-- الحل: خانة `order_id` صريحة. الصفوف القديمة هتفضل بالنص (بنجمع الاتنين).
-- ==========================================================================

alter table activity_log
  add column if not exists order_id uuid;

create index if not exists activity_log_order
  on activity_log (order_id, created_at desc);


-- ===== تأكيد =====
--   select count(*) filter (where order_id is not null) as مربوط,
--          count(*) filter (where order_id is null) as بالنص
--   from activity_log;


-- ==========================================================================
-- الرجوع
-- ==========================================================================
-- drop index if exists activity_log_order;
-- alter table activity_log drop column if exists order_id;
