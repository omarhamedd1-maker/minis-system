-- ==========================================================================
-- قالب رسالة «اسأل بعد التسليم»
-- --------------------------------------------------------------------------
-- النص اللي بيروح للعميل بعد ما يستلم. كان مكتوب في الكود، يعني صاحب المتجر
-- ماكانش يقدر يغيّر كلمة فيه — والكلام ده بيروح لعملاءه هو بصوته هو.
--
-- الفاضي معناه «استخدم الافتراضي» (`lib/message-template.ts`) — مش «مافيش
-- رسالة».
-- ==========================================================================

alter table tenant_credentials
  add column if not exists followup_template text;
