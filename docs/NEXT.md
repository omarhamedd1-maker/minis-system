# اللي على الدور — تسليم للشات الجديد

اقرا `HANDOVER.md` و`PLAN.md` الأول. الملف ده هو **الطلبات المفتوحة** من عمر
بالترتيب، ومعاها حاجات اتكشفت في الشغل ولسه ماتعملتش.

**آخر تحديث: ٥ أغسطس ٢٠٢٦** — بي آر #9 لـ #25 اتدمجوا كلهم.

---

## أول حاجة لازم تعرفها

**الأرقام اللي في الملف ده اتغيّرت.** رسوم بوسطة بقت بتتقرا **حقيقية من
كشف حسابها** بدل ما نقدّرها، فأي رقم قديم عن التكلفة أو الأرباح مش صالح.

وطريقة الشغل مع عمر: **يجرّب على تليفونه ويقول** — الإشعارات كلها اتظبطت
كده، إشعار تجريبي ورا إشعار تجريبي وصورة من شاشة القفل.

---

## ١. الدومين لكل متجر — **البند الوحيد الكبير الفاضي**

عمر عايز كل متجر ليه صفحة دخول خاصة. **القرار اتاخد (٤ أغسطس):**

- **ساب دومين لكل متجر** — `minis.الموقع.com` · `shop2.الموقع.com`
- **كل متجر حساباته منفصلة** — نفس الإيميل ينفع يتسجّل في متجرين
  بباسوردين مختلفين

**وحاجتين موقّفينه:**

**أ) مافيش دومين.** الموقع على `minis-system.vercel.app` وده **مايتعملوش
ساب دومينات**. لازم دومين حقيقي يتشترى ويتربط في فيرسل. **ده قرار عمر.**

**ب) إيميلات سوبابيز فريدة على مستوى المشروع كله**، فنفس الإيميل مايقدرش
يبقى حسابين. الحل المقترح: السيستم يخزّن الإيميل داخليًا بشكل مختلف لكل
متجر (`omar+minis@…`) والمستخدم مايشوفش ده — بيكتب إيميله العادي وخلاص.

**✅ اتعمل (٦ أغسطس) — كل اللي مايحتاجش دومين:**

- **`tenants.slug`** — اسم مختصر لكل متجر، بيتظبّط من شاشة البيزنسات
- **`/login/<slug>`** — صفحة دخول باسم المتجر
- **الحسابات المنفصلة** — الإيميل بيتخزّن مبوّب (`omar+minis@…`) والمستخدم
  مايشوفش ده. نفس الإيميل بقى ينفع في متجرين بباسوردين

⚠️ **`sql/tenant-slug.sql` لازم يتشغّل.** من غيره السيستم بيشتغل بالسلوك
القديم (مفيش تبويب ولا صفحات متجر) — مافيش حاجة بتقع، بس الميزة مش شغالة.

**اللي لسه ناقص للساب دومين**: شراء الدومين + ربطه في فيرسل + وسيط بيقرا
أول جزء من الدومين ويحوّله لنفس المسار. **مافيش حاجة تانية تتكتب.**

---

## ٢. قرارات مستنية عمر — مش كود

**اتقفلوا بقرار عمر (١٠ أغسطس) — متتفتحش تاني:**
- ~~الـ١١ أوردر اللي تحصيلهم مختلف عن بوسطة~~ (كان فرق ٨٬٣٤٥ ج)
- ~~البنود اللي بتكلفة صفر~~ (كانت ٣ بنود)

قال «سيبك منهم خالص ومتجيبش سيرتهم تاني».

**الأرقام دي اتعدّت على الداتا الحقيقية ٦ أغسطس** — القديمة كانت مضخّمة.

| الحاجة | الرقم |
|---|---|
| أوردرات ماعدّوش على بوسطة أصلًا | ٣٥ — كلهم **متسلّمين**، من ١٥ فبراير لـ١٧ يوليو |
| **قيمة البضاعة في الشحنة** | إحنا مابنبعتهاش، فكل الشحنات متأمّنة بـ**٢٠ ج** مهما كانت غالية. لو بعتناها التغطية تبقى كاملة والرسوم تزيد ١٪ |
| ~~الباقة ماغطّتش ١٩٪~~ | **اتحسمت — مافيش مشكلة.** ٢٩ من الـ٣٩ كلهم **فبراير قبل ما تشترك في الباقة أصلًا**. الباقي ١٠ على ٥ شهور = ٢٪ |
| **قسط الباقة على الأوردر** | **اتحسم ٦ أغسطس**: عمر بيسجّل الباقة مصروف شهري بإيده وبيغيّر حجمها من شهر للتاني. فشاشة الأوردر بقت تعرض نصيب الشحنة من الباقة (من `bosta_ship_fee_real`) عشان الشحنة اللي الباقة غطّتها ماتبانش أرخص بمية جنيه من اللي ماغطّتهاش — **والأرباح سابتها زي ما هي** (`orderCarrierCost`) عشان الباقة ماتتحسبش مرتين. `BOSTA_BUNDLES` و`bundlePerOrder` في `lib/format.ts` لسه ميّتين ومحدش بينده عليهم |

---

## ٢.٥ ⚠️ تطبيق شوبيفاي لازم يبقى Public — بند بيوقف البيع

**اتكشف ١٠ أغسطس وإحنا بنركّب البيزنس التاني.** الربط طلّع صفحة ٥٠٠ من
شوبيفاي، والإعدادات كلها كانت سليمة (`legacy install flow: true` ·
`embedded: false` · الـRedirect URL مظبوط · الصلاحيات مطابقة).

**السبب**: تطبيق `Minis System` توزيعه **Custom** — يعني بيتركّب على
**متجر واحد بس** ومقفول عليه. اشتغل مع مينيز (`d8rtv0-uq`) وبس، ولما
جينا على متجر ٢ سِك (`dgcjpd-xw`) شوبيفاي طلّعت ٥٠٠ على صفحة `/app/grant`.

⚠️ **وشوبيفاي مابتخليكش تغيّر التوزيع بعد ما تختاره** — التطبيق ده عمره ما
هيركّب على متجر تاني.

**المطلوب**: تطبيق **جديد بتوزيع Public**، ومفاتيحه تتحط مرة واحدة في
`الإعدادات ← تطبيق شوبيفاي`. بعدها أي عميل بيكتب دومينه ويدوس «اربط» —
**والكود جاهز، مسار OAuth شغّال واتجرّب**. اللي ناقص تسجيل التطبيق بس.

(وفيه تطبيق تاني في اللوحة اسمه `Minis System 3` — يتشاف توزيعه الأول،
يمكن يكون Public خلاص.)

**الحل المؤقت لمتجر معروف**: تطبيق خاص جوّه المتجر نفسه (Settings ← Apps
← Develop apps)، وياخد الصلاحيات الأربعة، ومفاتيحه تتحط في إعدادات
البيزنس. السيستم بيشتغل بيه عادي (`grant_type: client_credentials`).
**بس ده مينفعش لعميل غريب** — ده شغل مبرمج.

### ⚠️ اتأكد بالتجربة (١٠ أغسطس) — متعيدش الدورة دي

اتعمل تطبيق جديد اسمه `mino` بكل الإعدادات الصح (`legacy install flow`
✅ · `embedded` ❌ · الصلاحيات في **Scopes** مش Optional · الـRedirect URL
مظبوط · نسخة **Active**). وبرضه رفض بـ **Unauthorized Access**.

**السبب الحقيقي**: التطبيق في حساب شوبيفاي بتاع `minishomedecoreg@` —
واللي فيه **متجر مينيز بس**. ومتجر ٢ سِك تحت **حساب شوبيفاي تاني**.

**وتطبيق من غير توزيع عام مايتركّبش غير على متاجر نفس الحساب.** عشان كده
اشتغل مع مينيز ورفض ٢ سِك.

**والدليل القاطع**: صفحة التطبيق فيها قسم `Installs` وجنبه **0**، وزرار
`Install app` بيفتح قايمة المتاجر — والقايمة فيها متجر واحد بس.

**الخلاصة**: مفيش إعداد ناقص. اللي ناقص **توزيع Public من شوبيفاي**، وده
بيعدّي بمراجعة منهم.

### ✅ اللي اتعمل عشان المراجعة (١٠ أغسطس)

عمر قرر: **مش هيعمل تطبيق جوّه حساب كل عميل** — يبقى التوزيع العام.

- **`/api/shopify/webhooks`** — الويب هوكس التلاتة الإجبارية
  (`customers/data_request` · `customers/redact` · `shop/redact`) ومعاهم
  `app/uninstalled`. التوقيع بيتحقق على **الجسم الخام** (base64 مش hex)،
  والتوقيع الغلط بيرجع **٤٠١** — شوبيفاي بتختبر ده بالظبط في المراجعة.
  والموضوع اللي مش بنرد عليه بيرجع **٢٠٠** عشان مايفضلش يتعاد.
- **`/privacy`** — صفحة الخصوصية، مفتوحة من غير دخول (شوبيفاي بتطلب اللينك).
- **شيل التطبيق بيمسح مفاتيح الربط بس** — مش داتا البيزنس. الأوردرات
  والعملاء ملك صاحب المتجر، ومسحهم بويب هوك معناه شغل بيضيع.

### ✅ التطبيق العام اتعمل خلاص (١١ أغسطس)

| الحاجة | القيمة |
|---|---|
| الاسم | `Mino - Store Operations` |
| Client ID | `9054cde984574430431ceb8fc03429fc` — **متحطّط في `shopify_app`** |
| المنظمة | `230451893` · التطبيق `409062670337` |
| النسخة | `mino-store-operations-3` — **Active** |
| التوزيع | **Public** — اتأكد واتقفل (مالهوش رجوع) |

⚠️ **الشرطة الطويلة `—` شوبيفاي بترفضها في اسم التطبيق.** ده السبب الحقيقي
اللي رفض بيه الاسم مرتين — مش الكلمات ولا الحروف الكبيرة. الشرطة العادية
`-` عدّت من أول مرة.

### ⛔ ومع كده ٢ سِك لسه مايتركّبش — وده مش عطل

**الأهم في الملف كله.** التوزيع العام **لوحده مش كفاية**: التطبيق لازم
**يعدّي المراجعة ويتنشر على المتجر** قبل ما أي متجر برّا حسابنا يقدر
يركّبه. قبل كده هو متاح لمتاجر **نفس الحساب بس**.

ده مش استنتاج — ده مكتوب في شوبيفاي:

> النوع اللي كان بيتركّب من غير مراجعة (`unpublished apps`) **اتلغى في
> ٩ ديسمبر ٢٠١٩**، «عشان ثقة التجار وأمانهم بنطلب مراجعة كل التطبيقات».
> <https://help.shopify.com/en/partners/help-support/faq/unpublished-app-deprecation>

**والدليل من اللوحة نفسها**: زرار `Install app` في التطبيق العام بيفتح
قايمة المتاجر — وفيها **متجر مينيز بس**. ٢ سِك مش فيها لأنه تحت حساب تاني.

⚠️ **وخلّي بالك من فخ**: لو جرّبت لينك OAuth بـ`curl` من غير تسجيل دخول
هيرجّع `303` لصفحة اللوجين — **وده مش دليل إن التطبيق مقبول**. شوبيفاي
بتحوّل للوجين قبل ما تبص على الـ`client_id` أصلًا. الاختبار الحقيقي لازم
يبقى بحساب داخل فعلًا.

**يعني في طريقين، والقرار قرار عمر:**

| | تستنى المراجعة | تعمل تطبيق **Custom** لكل عميل |
|---|---|---|
| ٢ سِك يتركّب امتى | بعد موافقة شوبيفاي | **دلوقتي** |
| مراجعة | لازم | **مالهاش لزوم** |
| بندخل حساب العميل؟ | لأ | **لأ** — التطبيق في حسابنا، بنبعتله لينك بس |
| بيشتغل على كام متجر | أي عدد بلينك واحد | **متجر واحد** — تطبيق جديد لكل عميل |

الكلام على `Generate link` في التوزيع الكاستم من:
<https://shopify.dev/docs/apps/launch/distribution/select-distribution-method>

### ⛔ التقديم نفسه مقفول ورا رسم $19 — **شغل عمر وحده**

`partners.shopify.com ← Mino ← Manage submission` بتوديك على
`/apps/register` مش على صفحة التقديم. المطلوب هناك:

- نوع النشاط (فرد / منظمة) وإقرار عن الحسابات المرتبطة
- الموافقة على **Partner Program Agreement**
- **رسم تسجيل لمرة واحدة $19** + طريقة دفع

⚠️ **المساعد مايعملش الخطوة دي** — بيانات دفع وموافقة على اتفاقية باسم عمر.
من غيرها مفيش صفحة تعريف تتملا ولا تقديم يتبعت، مهما كان الباقي جاهز.

### اللي لسه ناقص للمراجعة — **شغل عمر مش كود**

- ~~الأيقونة~~ ✅ **اترفعت** (١١ أغسطس). الصورة من `/api/brand-icon`.
  ⚠️ المساعد مايقدرش يرفعها — أداة الرفع في المتصفح مكسورة. متضيّعش وقت.
- ~~الصور~~ ✅ **خمس صور ١٦٠٠×٩٠٠ جاهزة** في `OneDrive/Pictures/mino-listing`
- النصوص كلها جاهزة في `docs/shopify-listing.md`
- **الباقي كله واقف على رسم الـ$19 فوق.**

### ✅ البيزنس التجريبي (١١ أغسطس)

`Mino Demo Store` · `/login/demo` · `demo+mino@minishomedecor.com`

٢٤ منتج · ٢٣٠ عميل · ٢٧٠ أوردر على ٧ شهور · مصاريف وخزنة. **صفر داتا من
مينيز** — كله متولّد. بيخدم صور صفحة شوبيفاي والمتجر التجريبي والشرح للعملاء.

السكريبتات في السكراتش (مش في الريبو): `seed-demo.mjs` بيزرع،
`delete-demo.mjs` بيمسح، `shot.mjs` بيصوّر ١٦٠٠×٩٠٠ بمتصفح مبرمج.

⚠️ **الأرقام في الداشبورد بتتحرك من صفر (`CountUp`)** — أي تصوير آلي لازم
يستنى النص يثبت، مش وقت ثابت.

⚠️ **حالات الأوردر بتتخزّن مفاتيح إنجليزي** (`delivered` · `cancelled` …)
مش النص العربي. النص للعرض بس في `ORDER_STATUS_LABELS`.

---

## ٣. شغل كود فاضي

**باقي دوال شوبيفاي** (استقبال الأوردر الجديد) لسه في لوحة سوبابيز — مفيش
تاريخ ولا رجوع ولا اختبار. نفس مشكلة دوال بوسطة قبل ما تتنقل.
⚠️ **الكود مش في الريبو** — لازم يتجاب من لوحة سوبابيز الأول.

**تقليل استخدام مفتاح الأدمن** (٢.٦ في `PLAN.md`) — ٤٣ ملف بيستخدمه.
النقل الكامل للعميل المحمي بالـRLS لسه ماتعملش، **بس الخطر الفعلي اتقفل**:

`lib/tenant-isolation.test.ts` بيقرا الكود نفسه وبيوقع لو حد كتب استعلام
قراءة بمفتاح الأدمن من غير فلتر بيزنس. اتجرّب بالعكس — شيلنا فلتر واحد
والاختبار وقع وقال الملف والسطر.

**وخمس تسريبات حقيقية اتصلّحت (١١ أغسطس):**

| المكان | كان بيعمل إيه |
|---|---|
| `app/api/costs/route.ts` | **الأخطر** — ملف التكاليف كان بينزّل أسعار وتكاليف **كل البيزنسات** لأي حد معاه صلاحية المنتجات |
| `products/actions.ts` (رفع التكاليف) | بيطابق الأسماء على منتجات بيزنس تاني |
| `orders/reconcile/actions.ts` ×٢ | فحص التغطية بيقارن شحنات البيزنس بأوردرات الكل |
| `orders/[id]/page.tsx` ×٢ | سجل الأوردر بيطابق **بنص رقم الأوردر**، وأوردر ١٤١٦ عند بيزنس تاني كان هيطلّع سطوره عندنا |

⚠️ **مافيش تسريب حصل فعلًا** — ٢ سِك كان لسه فاضي (صفر منتج وصفر أوردر)
وقت الإصلاح. بس كان هيحصل أول ما يشتغلوا.

**مرحلة ٥**: البيزنس التاني يعدّي التركيب لوحده كعميل غريب، وكل حاجة
نضطر نعملها بإيدنا تتسجّل كعيب.

---

## ٤. حاجات على عمر

- ⚠️ **`sql/tasks-reminders.sql`** — لازم يتشغّل عشان التنبيهات والتكرار
  الكاستم يشتغلوا أصلاً. من غيره الشاشة بتحفظ والأعمدة مش موجودة.
- ⚠️ **جدولة `tasks-tick-15min`** — الأمر متعلّم عليه في آخر نفس الملف.
  **المسار `/api/tasks/recur` بيعمل التوليد والتنبيهات الاتنين ومحدش
  بيناديه لسه** — يعني التنبيه اللي تظبّطه مش هيرن.
- تشيل الدالة القديمة `shopify-order-push` من لوحة سوبابيز (ميعادها عدّى)
- **Rotate** لمفتاح سوبابيز السري (اتكتب في محادثة قبل كده)
- سوبابيز مدفوع → نسخ احتياطية يومية + مشروع تجريبي
- **يشيل التطبيق من الشاشة الرئيسية ويضيفه تاني** عشان ياخد اسم `MINIS`

---

## ٥. مؤجّل بقرار عمر

**الشحنات القديمة المش مربوطة** — قال «سيبك منها خالص». الرقم الحقيقي
٥٩ شحنة بدون مرجع: ٢١ مربوطة برقم تتبعها خلاص، ٢٠ ينفع تتربط بالتليفون،
١٨ مالهاش أي أثر.

---

## قواعد الشغل مع عمر

- **عربي مصري بس** — مافيش خلط بإنجليزي في الشات.
- **البي آر شغل المساعد**: يعمله ويستنى الفحص يخضّر ويدمجه بنفسه.
  عمر مش بيدوس ميرج. (مافيش `gh` على الجهاز — البي آر بيتعمل من المتصفح.)
- **ملفات SQL عمر بيشغّلها بإيده** — المساعد بيكتبها بس.
- **اعرض قبل ما تنفّذ** في أي حاجة بتلمس داتا — `?dry=1` الأول.
- **أوبريكس مرجع للمشاكل بس** — ممنوع تقليد شكله أو تقسيمته أو كلامه.
- **جرّب على تليفونه**: أي حاجة في الإشعارات، ابعت إشعار تجريبي واطلب صورة.
